import React, { Component } from 'react';
import './PMs.css';

class PMs extends Component {
    render(){
        return(
            <h1>Your private messages!</h1>
        );
    }
}

export default PMs;